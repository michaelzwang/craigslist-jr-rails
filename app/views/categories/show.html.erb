<h1><%= @category.name %></h1>

<ul class="category-articles">
<% @category.articles.each do |article| %>
    <li>
    <%= link_to "#{article.title}", article %>: $<%=article.price%>, posted: <%=article.updated_at.strftime("%Y-%m-%d")%>
    </li>
<% end %>
</ul>

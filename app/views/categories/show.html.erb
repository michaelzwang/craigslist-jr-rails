<h1><%= @category.name %></h1>

<% @category.articles.each do |article| %>
  <ul>
    <li>
    <%= link_to "#{article.title}", article %>: <%=article.price%>, <%=article.updated_at%>
    </li>
  </ul>
<% end %>
